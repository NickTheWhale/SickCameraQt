1. install Qt 6.5.1_msvc2019_64
    a. do this one first, it takes awhile

2. install opencv 4.8.0
    a. set environment variable "OPENCV_DIR" to where you install

3. install "Visual Studio 2022"
    a. install "Desktop development with C++" workload
    b. install "Qt Visual Stuido Tools" extension
    c. set the qt version in the extension

4. clone (or download zip) of this repo

5. open .sln